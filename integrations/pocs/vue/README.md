# Vue 3 POC

Goal: build Agent Neo as a custom element using Vue 3's defineCustomElement.

Why Vue:
- Strong DX and ecosystem.
- Native custom element support.

Next steps:
- Scaffold Vue 3 + Vite.
- Build custom element bundle for Angular.
- Validate runtime isolation with pdfjs.
